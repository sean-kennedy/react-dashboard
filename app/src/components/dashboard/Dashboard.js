import React from 'react';

const Dashboard = React.createClass({
    render() {
        return(
            <div>
                <h1>Dashboard</h1>
            </div>
        )
    }
});

export default Dashboard;